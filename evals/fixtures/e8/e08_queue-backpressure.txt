The queue does have backpressure, although it is not switched on by default which is why you may not have noticed it. The relevant setting is `queue.max_inflight` and it currently sits at a value of zero, and zero means unlimited. If you set it to something like 200 then producers will start blocking once there are 200 messages in flight, which is the behaviour you were describing wanting.
