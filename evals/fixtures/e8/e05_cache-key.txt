Simply put, the cache is never seeing the second request at all, and the reason for that is that the cache key currently includes the request id. Since every request has a different id, every single lookup is a miss by construction. What you want to do is drop the request id from the key, which is built in `lib/cache.js:41`, and once you do that the hit rate should recover fairly quickly.
