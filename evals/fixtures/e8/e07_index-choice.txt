There are really a couple of different options available to you here and it is worth walking through both of them. The first option is a single composite index on the two columns, which is going to help the query you showed me but will cost roughly 200 MB of additional storage. The second option is a partial index restricted to active rows only, which is much smaller at around 12 MB but only helps when the query filters on active.
