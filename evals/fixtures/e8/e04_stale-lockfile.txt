The build failed, and the reason that it failed is that the lockfile was stale. What happened is that a dependency was added to the project without anyone running a reinstall afterwards, and so the resolver never got the chance to write the new entry into the lockfile. To summarize, the fix here is that you need to run install first, and then rebuild afterwards, and things should go green again.
